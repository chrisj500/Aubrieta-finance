services:
  open-finance:
    build: .
    ports:
      - "3000:3000"
    env_file: .env
    environment:
      - BIND_ADDRESS=0.0.0.0
    volumes:
      - ./data:/app/data
    restart: unless-stopped
